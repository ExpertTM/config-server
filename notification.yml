spring:
  application:
    name: NOTIFICATION-SERVICE

kafka:
  topic-properties:
    name: ${NOTIFICATION_TOPIC}
    partitions: 3
    replicas: 3
    min-in-sync-replicas: 2
  retry:
    interval: 1000
    max-attempts: 2
